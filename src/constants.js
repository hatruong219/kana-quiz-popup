const INTERVAL_MS = 1.5 * 60 * 1000
const TICK_MS = 30 * 1000

const CLOSE_DELAY_CORRECT_MS = 1500
const CLOSE_DELAY_WRONG_MS = 60 * 1000

module.exports = { INTERVAL_MS, TICK_MS, CLOSE_DELAY_CORRECT_MS, CLOSE_DELAY_WRONG_MS }
